from writersroom.relationship.relationship_record import (
    RelationshipRecord,
)


class RelationshipParser:
    """Parses relationship analyser output."""

    def parse(
        self,
        response: str,
    ) -> list[
        RelationshipRecord
    ]:
        """Parse relationship records."""

        #
        # We'll implement the parser
        # after we've finalised the
        # prompt format.
        #

        return []